[
  {
    "name": "/help lists available commands",
    "steps": [
      {
        "send": { "text": "/help" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Available commands:\n/start — Start the bot\n/help — Show this help\n/tip — Get a random programming tip\n/count — Show total tips served" } }
        ]
      }
    ]
  },
  {
    "name": "unknown command replies with friendly fallback",
    "steps": [
      {
        "send": { "text": "/nonsense" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Use /tip for a kind tip, /count to see how many tips have been served." } }
        ]
      }
    ]
  },
  {
    "name": "plain text still works",
    "steps": [
      {
        "send": { "text": "Hello there" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "I received your message." } }
        ]
      }
    ]
  }
]
